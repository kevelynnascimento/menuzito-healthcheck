name: Keep API Awake

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: |
          curl -fsS https://menuzito-api.onrender.com/health
          sleep 10
          curl -fsS https://menuzito-api.onrender.com/health
          sleep 10
          curl -fsS https://menuzito-api.onrender.com/health